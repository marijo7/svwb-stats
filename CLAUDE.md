# CLAUDE.md

svwb-stats を Claude Code で触るときの前提。プロジェクトの説明は README.md にある。

## このプロジェクトの決めごと

- **依存を増やさない。** Python 3.9 以降の標準ライブラリだけで動く。`pip install` も
  `npm install` も要らない状態を保つ (Windows ユーザーが `start.bat` のダブルクリック
  だけで使えることが前提)。ブラウザ側もビルド不要の素の HTML / CSS / JS。
- **数字の計算は Python 側だけ。** 勝率などは `compute_stats` にしかない。ブラウザは
  `/api/stats` の結果を描くだけにする。CLI とブラウザで数字がずれないため。
- **戦績は 1 行 1 試合の JSONL。** 追加は追記のみ。git の差分がそのまま「その日足した
  試合」になる形を崩さない。
- **設定は config.json。** クラスやグレードを増やすのにコード変更が要らない状態を保つ。
- コメントと UI の文言は日本語。既存の書き方に合わせる。

## 確認の仕方

- テストは `python3 -m unittest discover -s tests` (約 0.8 秒)。Python を触ったら走らせる。
- **テストは frontend をほぼ見ていない。** `web/*.js` と `style.css` のカバレッジは実質
  ゼロで、`/` が 200 を返すかを見ているだけ。CSS や JS だけの変更でテストが緑でも、
  何も確認できていない。実際に動かして確かめること。
- 画面の確認はサーバーを別ポートと**別のデータファイル**で立てて行う
  (`python3 svwb.py --data /tmp/.../scratch.jsonl serve --port 8899`)。
  `data/records.jsonl` は本番の戦績なので触らない。
- 確認は DOM とテキストの assert を主にする。**スクリーンショットは高くつく**ので、
  レイアウトが崩れていないか目で見ないと分からないときだけ、変更箇所を `clip` で
  切って 1 枚。フルページの縦長スクショを何枚も撮らない。
- 検証用スクリプトは 1 本を使い回す。毎回新しく書き起こさない。
- ファイルは範囲読み (`offset` / `limit`) と grep を使う。全文読み込みと
  `git diff` の全文出力は避ける。

## 報告

- 結論と、判断が要る点だけを短く。テストの件数を毎回書かない。
- frontend の変更では「テストが通った」ではなく、実際に確認した内容を書く。
